# Superseded

D&D Beyond import now lives in the app itself: **New character > Import >
"From D&D Beyond"**, backed by `src/utils/dndbeyond/` and
`app/api/dndbeyond/[id]/route.ts` (see that folder's README for details).

This `scripts/import/dndbeyond/` folder is no longer used - the files here
were left in place (rather than deleted) only because this session has no
way to delete files on your machine. Safe to delete this whole folder.
